
function Header() {  //can also add export default to this line instead
    return (
        <header>
            <h1>React Movies</h1>
        </header>
    )
}

export default Header; //Does the same thing as adding export default infront of function